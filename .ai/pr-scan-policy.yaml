mandatory_checks:
  security:
    - "bandit-scan"

  build:
    - "compile"

  ci:
    - "angular-job"
    - "python-job"

informational_checks:
  - "Analyze (python)"
  - "Analyze (actions)"
  - "Analyze (javascript-typescript)"
  - "CodeQL"
  - "init-check"
